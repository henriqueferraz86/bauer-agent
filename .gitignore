# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/

# Virtualenv
.venv/
venv/
env/

# Pytest / Coverage
.pytest_cache/
.coverage
htmlcov/

# MyPy
.mypy_cache/

# Bauer Agent — runtime e dados locais
# config.yaml é do USUÁRIO (gerado por `bauer setup`). Versioná-lo fazia o
# git reset --hard do --update apagar a config de quem instalou. Template
# fica em config.yaml.example.
config.yaml
.runtime_state.json
.orchestrate_progress/
.bauer_active_company
logs/
# ANCORADO: sem a barra inicial, `workspace/` casava com QUALQUER diretorio
# desse nome em qualquer profundidade — inclusive `bauer/core/workspace/`, que
# e codigo. O pacote existia no disco, nao entrava no git, e o wheel saia sem
# ele: `ModuleNotFoundError: bauer.core.workspace` so no ambiente instalado.
# A regra sempre quis dizer o sandbox da RAIZ do repo.
/workspace/
memory/
specs/
companies/

# Coverage reports
coverage.json
coverage.xml
htmlcov/

# Artefatos de teste / build soltos
MagicMock/
psutil/
psutil-7.2.2.dist-info/
pytest-cache-files-*/
# uv.lock É versionado (install reproduzível) — ver plano 023 #13.
tmp/
test_request.json

# Claude Code
.claude/

# Segredos
.env
*.key
*.pem

# IDEs
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db
_quantfx_staging/

# Graphify — grafo do codebase, regenerável (python -m graphify . --code-only)
graphify-out/

# Worktrees temporarios do proprio Bauer (scratch, nunca versionado).
.bauer_worktrees/
